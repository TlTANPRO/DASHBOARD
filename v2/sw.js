// sw.js — DISABLED. PWA offline cache disabled to prevent stale code blocking fixes.
// To re-enable: uncomment below + bump CACHE name.
// self.addEventListener("install", () => self.skipWaiting());
// self.addEventListener("activate", (e) => e.waitUntil(self.clients.claim()));
